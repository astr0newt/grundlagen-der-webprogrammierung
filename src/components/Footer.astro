---
---

<!-- Footer -->
<div class="flex gap-16 border-t-1 border-black p-8">
  <!-- Flexbox -->
  <div class="basis-1/3">
    <!-- Left side -->
    <p class="font-sans text-base text-slate-900 mb-4">
      Nichts mehr verpassen!
    </p>
    <p class="font-serif font-semibold text-2xl">
      Jeden Monat neue Rezeptideen mit unserem Newsletter!
    </p>
  </div>

  <!-- Right side -->
  <form class="basis-2/3" action="newsletter_subscribe.php" method="post">
    <!-- Grid -->
    <div class="grid grid-cols-2 gap-4 mb-4">
      <!-- Flexbox item -->
      <input
        type="text"
        name="fullname"
        placeholder="Vorname Nachname"
        min-length="5"
        required
        class="col-span-1 h-12 border-1 border-black bg-transparent text-black px-4 focus:outline-0 placeholder:text-black"
      />
      <input
        type="email"
        name="email"
        placeholder="e-mail-adresse@provider.de"
        min-length="5"
        required
        class="col-span-1 h-12 border-1 border-black bg-transparent text-black px-4 focus:outline-0 placeholder:text-black"
      />
    </div>
    <div class="col-span-2">
      <input
        class="mb-4 w-full bg-black text-white h-12 hover:bg-slate-800 ease-in-out duration-200 hover:pointer-events-auto"
        type="submit"
        value="Abonnieren"
      />
      <p class="text-center font-sans text-xs">
        🤑 Für jeden neuen Abonnenten spenden wir 1 € an Klimaschutz-Projekte.
      </p>
    </div>
  </form>
</div>
